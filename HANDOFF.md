# HANDOFF — build status ledger & pickup guide

The living backlog for the Orange Replay build campaign. **Anyone (human, Claude session, Codex run) picking up this repo starts here.** Tick a box only when the task's Definition of Done below is met. Every pending item points at the docs that fully specify it — do not invent scope beyond those pointers.

How this document is used:

- **If the orchestrating session runs out of tokens/context**: hand this file to whoever continues. They work the first unticked item top-to-bottom, following the Working protocol below.
- **Work done by others is provisional**: mark it `[~]` (done, unverified) with the commit hash. The returning orchestrator re-verifies (gates + scope + judge pass) and promotes to `[x]`.
- **Keep it current**: whoever completes or dispatches a task updates this file in the same commit.

## Authority map (read before changing anything)

| Doc                       | Role                                                                                                           |
| ------------------------- | -------------------------------------------------------------------------------------------------------------- |
| `ARCHITECTURE.md`         | Design authority — system behavior, invariants, cost model                                                     |
| `PLAN.md`                 | Execution contract — ground rules, task scopes, logging contract, testing strategy                             |
| `design-final.html`       | **UI visual authority** (static mock, never modify/reformat) — judge all dashboard UI against it by screenshot |
| `docs/design-language.md` | The mock codified: tokens + per-component treatments for implementing UI                                       |
| `docs/specs/`             | Per-task dispatch specs (the exact contract each task was/will be built to)                                    |
| `CLAUDE.md`               | Conventions + toolchain (`vp`) usage                                                                           |

## Definition of Done (every task)

1. `vp check` and `vp test` green from repo root (`export PATH="$HOME/.vite-plus/bin:$PATH"`).
2. Scope respected (only the task's file budget touched; verify with `git status` — never trust an agent's report without checking the disk).
3. For UI tasks: screenshot comparison against `design-final.html` (boot the app, judge each element per `docs/design-language.md`).
4. Wide-event logging contract honored (PLAN.md §Ground rules 5) and cost invariants intact (§4).
5. Commit with a truthful message; tick here in the same commit.

### Visual judging protocol (learned 2026-07-04 — the replay-crop miss)

Judging under flattering conditions hides geometry bugs. The T3.5 replay embed was missing scale-to-fit entirely, but because sessions were recorded in the SAME browser window the dashboard was judged in, recorded width ≈ stage width and the bug presented as "minor letterboxing" instead of the top-left crop every real user would see. Rules:

- **Vary the geometry.** Any surface that renders captured/scaled content (replay viewport, thumbnails, embeds) must be judged with a deliberate mismatch: record at a large viewport, view in a small stage (and vice versa). Same-size testing proves nothing about scaling.
- **DOM assertions ≠ visual correctness.** The e2e proving "the right text exists in the replay iframe" says nothing about whether it's visible. For geometric bug classes, assert bounds (rendered content fits inside its stage) — see `docs/specs/fix-p3b-scale.md`.
- **Don't downgrade symptoms whose cause you haven't identified.** "Top-anchored letterbox, polish later" was a symptom of a missing transform, not a styling nit. A visual oddity goes on the punch list only AFTER its mechanism is understood.

## Status ledger

### Current local tooling and audit work

- [~] Analytics erasure lifecycle owner (2026-07-17) — spec `docs/specs/analytics-erasure-lifecycle-owner.md`. One deep Worker module now owns every `analytics_deletion_jobs` mutation and transition that was previously split across the sweeper, deletion journal, deletion V2, and purge jobs. Deletion requests update `session_deletions`, the analytics job, and the export outbox in one D1 batch while preserving the earliest request time, first known session start, sticky warehouse tombstone need, latest reason, and physical-completion reset. The legacy journal, warehouse watermark, 45-minute claims, two zero observations at least ten minutes apart, 23-hour alert, and independent V2 delivery/visibility state keep their existing behavior; a physically completed job remains eligible for V2 delivery. Architecture checks reject new job-table mutations outside the owner. Review found no behavioral issue. `vp check` passes with only the four known rrweb warnings, 78 focused Worker tests and 8 Spark runner tests pass, all 1,208 tests pass, and the Worker dry-run build succeeds. No schema, migration, API, UI, timing, dependency, configuration, deployment, or production data changed, and no deploy was run.

- [~] Finalized-session semantics owner (2026-07-17) — spec `docs/specs/finalized-session-semantics-owner.md`. One deep query module now owns the exhaustive finalized-session filter mapping, `Unknown error` fallback, error-row need, friction formula, all five sorts, cursor parsing/encoding, and null-last page behavior. Small D1 and R2 ports keep their deliberate differences: D1 still uses prepared bindings, live-table subqueries, and prefix ranges; R2 still uses escaped literals, existing CTE/dedup/deletion rules, substring prefixes, and flat expressions for its depth limit. Successful shadow comparison now ignores only D1's `has_checkpoint` value while common fields, row order, and cursors remain strict. Review caught and fixed cursor-error precedence before commit. `vp check` passes with only the four known rrweb warnings, 93 broader related tests and all 1,204 tests pass, and the Worker dry-run build succeeds. No schema, migration, response, cache, configuration, dependency, or UI changed, and no deploy was run.

- [~] Public-page publication owner (2026-07-17) — spec `docs/specs/public-page-publication-owner.md`. One deep Worker module now owns all `project_public_pages` and `public_page_sessions` SQL, publication revisions, stable replay ids, enabled-page checks, deletion fences, and public replay lookup. The Settings and anonymous-page modules are HTTP and response adapters. One shared privacy helper reduces entry URLs to a safe path in the Settings picker, public summaries, and public analytics. Architecture checks reject publication-table access outside the owner. Tests prove URL scrubbing, remove/unpublish/delete revocation, replay-id preservation, stale writes, and exact 404/503 error precedence. `vp check` passes with only the four known rrweb warnings, all 1,201 tests pass, and the Worker dry-run build succeeds. No migration, dependency, binding, route, deployment, or public response field changed, and no deploy was run.

- [~] SDK recording-session transition owner (2026-07-17) — spec `docs/specs/sdk-session-transition-owner.md`. `WorkerSink` now owns both idle wake-ups and closed-ingest acknowledgements in one queue: it waits for the active upload, makes one bounded final old-session flush, lets a close win over idle reconciliation, then changes the session id, resets the pipeline, and requests a required full snapshot in that order. The former callback coordinator, the independent closed-ack rotation fallback, and the unused duplicate transport callback are removed; inline transport inherits the same owner, and the checkpoint adapter is required so a constructor cannot silently omit the post-change snapshot. Real sink/session tests cover old/new ids and sequence zero, same-cookie idle, close-during-idle, continuously active capture, stop/drop/worker-failure cancellation, and full `init()` wiring. The active 35.50 KiB IIFE gate was not raised: the built artifact is 36,225 bytes gzipped, 58 bytes smaller than the 36,283-byte starting artifact. `vp check` passes with only the four known rrweb warnings, all 79 focused SDK tests and all 1,195 current-tree tests pass, and the SDK budget gate passes. No deploy was run.

- [~] DeepSec security remediation (2026-07-17) — fixed all eight security-relevant findings from `docs/audits/deepsec-2026-07-15.md` without changing the intended customer behavior: stale public-page drafts fail with a revision conflict; replayed live tickets are consumed before snapshot work; finalization and live replay decoding are bounded; accepted pre-finalization storage has a durable quota ledger; malformed replay events fail closed; blocked constructed stylesheets remain private; and analytics reports use no-follow, create-once, hash-bound publication. Migrations `0021_public_page_mutation_token.sql` and `0022_accepted_usage_reservations.sql` are mirrored for self-hosting. Both were applied to `orange-replay-idx-00-prod` on 2026-07-17; Wrangler reports no pending migration, and the new production column, table, and indexes were verified read-only. The Worker was not deployed. Its first security deploy must keep `ACCEPTED_USAGE_RESERVATIONS=0`; enable the flag only after the compatible consumer is live and the old consumer is gone. Independent reviews closed the retry, sparse-checkpoint, legacy-timestamp, and stylesheet-adopter gaps found during implementation. A fresh onboarding-free DeepSec snapshot using Codex `gpt-5.6-sol` at `xhigh` confirmed all eight fixed across runs `20260717060933-fd3bbc388ebed464` and `20260717062635-fdc0903511f10142`; five separate documented non-security bugs remain true, and the old Durable Object partial-commit report remains a false positive. `vp check`, all 1,176 tests, React Doctor 100/100, schema and mirror checks, SDK budgets, package/Worker builds, deployment assets, and the production-configured Wrangler dry run pass.

- [~] Ten-minute inactivity boundary and finalized-only customer demo Sessions (2026-07-17) — spec `docs/specs/fix-demo-finalized-sessions-and-idle-timeout.md`. The single shared close value is now 10 minutes, so SDK idle rotation and cookie expiry, the Session Durable Object close alarm, Presence cleanup, and provisional-head expiry stay aligned without another timer. When an active Session Durable Object starts during a rolling deploy, it now pulls an obsolete later stored alarm forward to the current deadline without changing tombstone purge alarms. The two-minute tail flush, 60-second Live threshold, private Sessions head overlay, and `/demo/live` remain unchanged. `/demo/sessions` now reads only finalized `/sessions` rows and does not request or merge provisional `session-heads`, so customer demo cards no longer sit in `Final details pending`. The nine changed TypeScript files pass scoped `vp check`; 35 focused SDK/dashboard/Worker-logic tests and all 14 Worker continuity tests pass. React Doctor reports no finding in this task's React file; its current 90/100 changed-tree score comes from concurrent Settings work outside this scope. Concurrent work also leaves the full repo check and suite non-green outside these files. No production deploy was run.

- [~] Brave browser detection fix (2026-07-17) — the ingest Worker previously classified browsers only from `User-Agent`; Brave keeps a Chrome-compatible user agent on desktop and Android, so those recordings were stored as Chrome. Browser classification now prefers Brave's `sec-ch-ua` brand and also recognizes the direct `Brave/` user-agent marker used by Brave platforms that expose it. Chrome remains Chrome when the Brave brand is absent. The SDK and ingest wire format are unchanged, so this adds no SDK bundle weight or custom CORS header. The focused classifier suite passes all 9 tests, `vp check` passes with only the four known rrweb warnings, all 1,085 tests pass, and the Worker dry-run build succeeds.

- [~] Zero-duration/ghost session fix (2026-07-17) — spec `docs/specs/fix-zero-duration-sessions.md`. Root cause was two-fold: session duration measured the span between server batch arrivals (a single-batch bounce always read 0:00 and every session undercounted by the first batch's buffer window), and the SDK's first flushed batch never contained the initial full snapshot, so a session closed before its second batch finalized as an unplayable Meta-only ghost that no pipeline layer filtered. The manifest is now a recorded-time artifact (bounds from clamped segment t0/t1, recorded start additionally capped at server start minus 5 minutes); the finalize message keeps the server bounds for D1 ordering/retention and carries optional `durationMs` + `hasCheckpoint` (consumer falls back for in-flight pre-upgrade messages); migration `0020_sessions_has_checkpoint.sql` adds the nullable D1 playability column (mirrored to the template) and it flows through a D1-only column list (`d1SessionRowColumns`) so warehouse SQL is untouched — warehouse rows read as unknown and adding the Iceberg column is a deferred follow-up under the cutover spec. The SDK holds timer/visibility flushes until the first full snapshot is buffered (pagehide, threshold, and memory drains stay ungated; the hold clears on snapshot arrival, any dispatched batch, and re-arms on rotation) — this cost 0.05KB gz on the IIFE bundle, which sat at exactly its 35.00KB ceiling, so the hard gate moved to 35.25KB with a dated note (warn stays 32KB). Dashboard: exact rows with `has_checkpoint=false` render the metadata-only treatment and the detail stage skips the player for them; fresh provisional rows show "Just started" instead of 0:00; session surfaces show `<1s` for sub-second recordings (playback clocks unchanged). `scripts/recover-session-durations.mjs` recomputes local D1 durations/playability from immutable manifests (dry-run default, backup before `--apply`); production re-export goes through the analytics backfill tooling, never raw warehouse updates. Regression tests cover the recorded span, clock skew, Meta-only finalization, queue compatibility, SDK first-flush behavior, formatting, and card states. Migration 0020 is applied locally and in production. Production recovery is complete in the replacement duration-v2 Pipeline and its three replacement warehouse tables; all live manifests validated and all required correction, coverage, sparse-event, and deletion rows were verified through the current recovery sequence. The deployed read path still targets the old failed tables, so the old resources and the paused watermark must remain until a Worker deploy switches reads to duration-v2 and verifies the new watermark. No raw warehouse data was edited.

- [~] Dashboard overview breakdown charts (2026-07-17) — replaced the flat breakdown lists with accessible horizontal charts, kept direct navigation to matching Sessions filters, added full-list dialogs, and added city as a project-scoped analytics dimension. City rows carry country context so same-named cities remain separate in SQL, filters, chart scale keys, flags, and navigation. The chart source is kept locally under its upstream MIT license, and only modules reachable from the overview entry points remain in the repo. Unused chart helpers and unused direct packages were removed during the audit.

- [~] Focused dashboard metrics with Number Flow (2026-07-16) — added `@number-flow/react` and kept the Overview metric sections mounted while their stats request is pending, so all eight focal values start at a real zero and flow to the fetched numbers. The Sessions result total, five account totals in the operator console, the selected public-recording count, and full recording-page summary metrics use the same restrained treatment. Repeated session cards, selected-session navigation, replay clocks and total duration, timeline counts and offsets, live counters, pagination, table rows, and breakdown lists remain instant so dense, frequent, or functional values stay stable. Counts, decimals, percentages, scroll depth, and grouped durations keep their formats; genuine missing analytics values still become an em dash. The wrapper uses a 240ms ease-out transition, keeps duration labels accessible, and respects reduced motion. Browser checks on the existing integrated server measured one Number Flow element on Sessions instead of 76, zero across all 25 session cards, six summary elements on the full replay with none in its controls or timeline, and ten rendered number parts across Overview's eight focal metrics; the checked pages had no horizontal overflow. `vp check` passes with only the four known rrweb warnings, all 1,062 tests pass, and the dashboard production build succeeds. React Doctor remains at 66 with only the existing timeline, tabs, and toast-lab findings.

- [~] Inset dashboard workspace shell (2026-07-16) — kept the Orange Replay brand, project, environment, and account header full width, then moved the route navigation, demo notice, and active page into one rounded dashboard frame with left, right, and bottom breathing room. The frame owns one shared `surface-2` background, `shadow-surface-2` elevation, and `SurfaceProvider` value for both navigation and page content; no separate inner elevation was added. The secondary navigation has no background override, so it computes transparent over the shared surface. The full-width account header is also transparent, allowing the dashboard's dotted base canvas to continue behind it instead of stopping at a solid chrome strip. Existing page widths stay centered inside the frame, with 12px outer spacing on desktop and 8px on narrow screens; the landing page and its files are unchanged. Browser checks at 1440x900 and 390x844 confirmed the full-width header, computed level-2 surface, continuous navigation background, inset frame, centered content, no page-level horizontal overflow, and no browser warnings or errors. The surface follow-up used the production static preview after its integrated `vp run dev` attempt was blocked before startup by the existing local D1 error `_cf_ALARM has 3 columns but 2 values were supplied`; no local data was changed. The focused app-shell check passes, all 1,061 tests pass, and the dashboard production build succeeds. The full `vp check` now passes with only the same four rrweb warnings. React Doctor reports no finding in `app-shell.tsx`; its existing findings remain in the changed timeline, tabs, and toast-lab files.

- [~] Dashboard shell density and SVG texture follow-up (2026-07-16) — replaced the wide CSS dot grid with the supplied 48px SVG tile's exact 3px square-dot geometry. The source's opaque light background was removed for dark-mode compatibility, and the working bundled pattern is set to 5% opacity with a muted light dot color; the equivalent SVG uses one reusable pattern cell instead of 256 repeated rectangles. The SVG lives in the dashboard source assets so Vite resolves and serves the real image in development and production; the earlier public-root URL fell through to the dashboard HTML shell and could not render as an image. The base canvas now uses a slightly deeper version of the original dashboard neutral, `oklch(0.135 0.004 285.857)`, while the raised workspace keeps `surface-2` and relies on its existing inset ring plus depth shadow instead of a separate solid border. This preserves the workspace depth without the severity of pure black. The full-width project/account header remains tightened from 12px to 8px vertical padding and now measures 46px in the browser while keeping the existing 26px avatar, horizontal spacing, and overflow behavior unchanged. The project selector now uses the existing subtle-border token instead of the brighter default control border, keeping it discoverable without pulling attention away from the workspace; a route-local height override reduces only this selector from 32px to 30px without changing the shared Select component. The demo selector now reads `Landing page`, while signed-in workspaces continue to show their real project names and retain the existing unavailable-account fallback. Independent layout assessment and pre/post mechanical scans found no arbitrary spacing or unresolved layout issues. Browser verification confirmed the native 48px SVG tile behind the top bar, with no horizontal overflow or warnings. The landing page remains unchanged.

- [~] Dashboard typography audit fixes (2026-07-16) — applied the approved typography pass to the real dashboard and removed the temporary `/typography-audit` comparison route. Page titles now keep supporting copy outside the heading, use a 1.1 line-height, and preserve the existing 18px scale; sentence-level supporting text now uses the readable muted color instead of the sub-AA dim token. Editable fields stay 16px below the `sm` breakpoint to prevent iOS focus zoom and return to 12px on wider screens. Truncated paths, locations, timeline details, client labels, and admin identities expose their full values. The dashboard now loads its variable sans face locally, root optical sizing is automatic, and standard `font-weight` replaces raw `wght`/`opsz` tags. Departure Mono remains the numeric face. Browser probes confirmed 16px/12px responsive inputs, the local font resource, no horizontal overflow, and the removed route returning Page not found. `vp check` passes with zero errors and the same four known rrweb warnings, all 1,058 tests pass, and the dashboard production build succeeds. React Doctor's changed-file scan still reports the pre-existing timeline memo, tab layout animations, and toast-lab size; `git show HEAD` confirms they predate this typography pass.

- [~] Shared React live badge (2026-07-16) — recreated the landing page's green pixel-light badge as reusable `LiveBadge` and `LiveDot` React components without changing the landing source or static design mock. The dashboard Live panel now uses the full 12px dotted badge in its heading and the source's 7px compact pinlight for dense rows. The 7x7 light field keeps the source colors, falloff, bloom timing, uneven twinkle, and reduced-motion behavior while using stable values for safe React renders. A follow-up browser measurement found that the copied opacity-only motion was running but looked static at 12px, so the bloom now visibly expands and each light shrinks as it dims; the compact row pinlight visibly breathes through the same bloom. Over a 1.2-second browser sample, the bloom grew from 0.83x to 1.12x while a pixel shrank from 1.08x to 0.78x and dimmed by about half, with no browser warnings or errors. Focused coverage verifies all 49 lights and the compact variant; React Doctor scores the changed React source 100/100. `vp check` passes with zero errors and the same four known rrweb warnings, all 1,062 tests pass, and the dashboard production build succeeds. Browser checks with a real local live session confirm the exact 12px/49-light badge and 7px/zero-cell row dot on desktop and at 390x844, with no horizontal overflow. Spec: `docs/specs/react-live-badge.md`.

- [~] Unified Cloudflare toolchain (2026-07-16) — the dashboard, Cloudflare Vite plugin, canonical Worker, local D1 commands, and production build/deploy commands now resolve the same Wrangler 4.110.0 release from the workspace catalog. The Worker also uses the matching v5 Cloudflare Workers types, leaving one Wrangler and one Workers-types version in the installed graph. The D1 migration helper no longer switches CLI workspaces between local and remote runs. A clean database applied all 18 migrations and the second run was a no-op; the 25-table Drizzle comparison passes. `vp check` has zero errors and the same four rrweb warnings, all 1,056 tests pass, the production asset build and Wrangler no-upload build pass, and the dependency audit reports no known vulnerabilities. The 13-case browser run passed 12 cases and all heavier snapshot benchmarks; its only miss was the 10,000-node timing sample crossing a single-frame threshold. Three of four isolated reruns passed, while one exceeded the frame-gap bound by 8 ms without a long task. No production migration or deploy was run.

- [~] Single-port local development (2026-07-16) — `vp run dev` now uses Cloudflare's Vite plugin to run the dashboard source, combined Worker, local D1/Durable Object/R2/KV bindings, landing page, public replay assets, and hot reload on one origin: `http://localhost:8787`. The old copied dashboard shell, standalone Wrangler process, default Vite API proxy, generated `wrangler.dev.jsonc`, and port `5200` workflow are removed from the current runbook; browser tests retain an explicit isolated proxy mode on random ports. The production asset and Worker deployment path remains separate and unchanged. Validation passes with one user-facing listener on `8787` and none on `5200` or `9229`, successful landing/dashboard/API/static-asset smoke requests, all 13 browser journeys, all 1,056 unit tests, the production asset build, and the Worker no-upload build. `vp check` has zero errors and the same four known rrweb warnings, the dependency audit reports no known vulnerabilities, and the only peer warning is the existing Better Auth/Drizzle version mismatch.

- [~] Dashboard performance profile (2026-07-15) — added a Brave/Playwright profiler that reuses the existing port 5200 server, disables browser cache per run, and measures overview paint and interactions, cold and warm route changes, request counts, duplicate API aborts, long tasks, heap, and scroll frames. Chrome DevTools measured the private overview at 375 ms LCP, 0 CLS, and 52 ms worst interaction latency across its tabs and date-range popup. The page's steady rendering and scrolling are healthy; its first-load cost is the dev graph (175 requests, 167 scripts, about 11.9 MiB with cache disabled), widened by the full icon and shared barrels plus one Strict Mode stats-query restart. Overview's frequent tab indicator now settles in 120 ms instead of 200 ms, while tab text, metric-row hovers, breakdown-row hovers, and card-edge feedback use a 100 ms ease-out instead of the former 150 ms treatment; Brave confirmed the resulting 120/100 ms animations and reduced-motion still disables them. The separate first Overview → Sessions visit also pulled the replay/player graph before a session was selected. Run `vp run @orange-replay/demo-site#profile:dashboard`; pass `DASHBOARD_PERF_URL` for a private local project and `-- --assert` to enforce budgets.

- [~] Departure Mono numeric system (2026-07-15) — bundled the official regular WOFF2 and SIL OFL license for both deploy surfaces, then removed the temporary font lab, all 48 generated candidate binaries, and the custom generator. Dashboard and landing metric digits, durations, timestamps, and counts now use Departure Mono; a digits-only face also covers numbers embedded in normal text without changing the surrounding Uncut Sans. Overview percentage symbols use Uncut Sans. Uncut Plan8 remains the face for code, URLs, keys, IDs, and labels. The dashboard now bundles Uncut Sans directly as its only sans face. Live desktop checks confirm the intended font families and weights. Final branch verification passes `vp check` with the same four existing rrweb warnings, all 1,056 tests, and every package production build.

- [~] Dashboard UI primitives (2026-07-15) — completed the Fluid tabs, input, select, switch, and surface primitives. Tabs support both value- and index-based selection, inherit or override their surface level, use transform-based indicators, and avoid render-time ref reads. Loading indicator geometry and timing now come from one exported settings object, so its tests verify the chosen configuration instead of hardcoding 4 px cells. `ToastLab` and `orange-toast.tsx` remain outside this work.

- [~] Repo-local agent hygiene (2026-07-15) — `.agents/` and `.codex/hooks.json` are ignored because they are machine-local Codex configuration. Shared source, audit reports, and project documentation remain tracked.

- [~] React Doctor Codex integration and cleanup (2026-07-15) — installed the local Codex skill and `doctor` package script; the exact React Doctor 0.7.8 changed-files audit now scores the dashboard 87/100 with no errors and 43 warnings, while public-page scores 100/100. All React Compiler blockers were removed. One timeline memo remains because its focused render-count test proves it is required; the two query-refetch effects coordinate head/watermark polling, the focusable scroll viewport supports keyboard use, `ToastLab` was deliberately left alone, and 41 warnings are non-component exports in the toast and generated-icon modules. `SessionsPanel` moved query synchronization into a dedicated hook, `PublicPageCard` moved both dialogs into a dedicated component module, and `SessionStage` now composes explicit variants instead of forwarding four booleans through three components. Verification: all 1,056 tests and all package production builds pass; `vp check` has no errors and retains four existing warnings in `packages/rrweb-fork`.

- [~] Architecture deepening pass (2026-07-15) — implemented all five reviewed boundaries without changing the normal product flow: finalized analytics reads now live behind one typed facade; project configuration delivery, durable storage, and ingest lookup have explicit owners; dashboard JSON requests and replay access share one Better Auth/demo access decision; account, project-key, stats, and session wire contracts are shared and decoded at the dashboard boundary; and the player now owns recorded/live lifecycle orchestration while the live-follow controller only owns transport. The old route-owned analytics cache helpers, duplicate project-config modules, and recorded-playback controller were removed. Malformed targeted API responses now fail with the clear `invalid_response` code; missing private sign-in configuration now shows the Better Auth unavailable state. `ToastLab` was not touched. Verification: `vp check` passes with zero errors and the same four known rrweb-fork warnings; all 971 tests pass; dashboard, Worker, player, and shared production builds pass. The changed-files React Doctor scan scores 77/100; its three compiler findings are the pre-existing `try/finally` sign-out handlers, and its 36 warnings are the known toast/generated-icon export reports. The dashboard JavaScript changed by about +6.5 KB raw / +1.6 KB gzip and the Worker by about +12.5 KiB raw / +2.4 KiB gzip. No production migration, deploy, or real GitHub OAuth browser flow was run.

- [~] Better Auth-only, analytics safety, and final security audit (2026-07-15) — Better Auth cookies are now the only private dashboard authentication path; the old shared/local token is removed from dashboard storage, headers, Worker private-route authorization, environment examples, deploy helpers, and docs. Anonymous demo access remains exact-project and read-only. Old remote token secrets are retired only through an explicit command after a real Better Auth login/key canary; normal deploys never delete them. Analytics reads now use actor/project/edge/global budgets, live-ticket limits fail closed, private config writes reject symlinks, and deletion v2 has a separate stream/table/readiness receipt with v1 fallback. The final code review found and fixed stale shared-package exports and active-tab replay-budget accounting. DeepSec 2.2.1 then ran read-only in an isolated secret-free copy with `gpt-5.6-sol` at `xhigh`; its 25 reports were manually triaged into 7 confirmed security vulnerabilities, 1 environment-dependent security risk, 16 non-security bugs, and 1 Cloudflare-runtime false positive. No DeepSec finding was auto-fixed; the full list is in `docs/audits/deepsec-2026-07-15.md`. Final gates: `vp check` has 0 errors and the same 4 rrweb warnings, 1051/1051 tests pass, all package builds pass, SDK budgets pass, D1 schema has 25 tables, the template mirror matches, and `git diff --check` passes. No production migration, deploy, old-secret retirement, or real Better Auth browser canary was run.

### Phase 0 — Bootstrap ✅

- [x] Monorepo scaffold, seed router, test harness — `bd45179`, `9a4d957`

### Phase 1 — Spine (W1) ✅

- [x] T1.1 shared wire/types/schemas/logger — `2d9ce77`
- [x] T1.2 SessionRecorder DO — `2a74905`
- [x] T1.3 ingest hot path — `3bb04ca`
- [x] T1.4 queue consumer + sweeper — `6eedeec`
- [x] T1.5 API worker routes — `2f00787`
- [x] T1.6 assembly, migrations, dev vars — `a041079`
- [x] T1.7 cross-module e2e — `bfb5c7e`
- [x] Judge loop (2 rounds, 19 findings fixed) — `cbd0190`, `c4f3d41`

### Phase 2 — Recorder fork + SDK (W2) ✅

- [x] T2.1 vendored rrweb capture fork @2.1.0 — `9e24860`
- [x] T2.2 SDK core — `c3e5ff6`
- [x] T2.3 off-main-thread pipeline — `ff71dea`
- [x] T2.4+T2.5 budgets, demo site, browser e2e — `8610b87`
- [x] Judge loop C1+C2 (privacy hardening, 4-scenario e2e) — `f52b562`, `762fc2d`

### Phase 3 — Dashboard + Player (W3) ✅

- [x] T3.1 dashboard scaffold — `124ea0a`
- [x] T3.1b reskin to finalized design language + judge round — `f10cc9f`, `b23f267`
- [x] T3.2 presence-registry DO, config write endpoints, install-verify — spec `docs/specs/t3.2-presence.md`; verified: lazy-TTL eviction (no alarms), throttled `waitUntil` pings, D1→KV config writes, 401s on all new routes; 194 tests green
- [x] T3.3 `packages/player` — spec `docs/specs/t3.3-player.md`; verified: worker-only decompression, upstream rrweb@2.1.0 (replay-only, no fork imports), 209 tests green. Known nuance: ORS1/live frames don't carry per-batch FLAG_UNCOMPRESSED — decoder tries gzip (magic bytes) then plain JSON; revisit if the wire ever adds per-batch flags.
- [x] T3.4 Live tab — spec `docs/specs/t3.4-live-tab.md`; visually judged vs mock's "Live now" panel with seeded presence sessions (rail treatment, pulse dots, meta lines, mono elapsed, row nav all match); visibility-gated 5s polling; 210 tests green
- [x] T3.5 session detail: player embed — spec `docs/specs/t3.5-detail-player.md`; judged with a REAL session recorded through the demo site + SDK: playback, error markers, amber playhead, sidebar seek, kbd handlers, segments table all verified in-browser; 222 tests green. Verification residuals fixed: T3.2's in-place edit of applied migration 0001 split into `0002_project_config.sql` (migration immutability — runtime column self-heal in project-config.ts stays as belt-and-suspenders; audit in T4.2); happy-dom pragma on replay-timeline tests; runtime `@/` import made relative (root runner can't resolve the alias cross-project — convention: runtime imports in root-tested dashboard libs are relative, `@/` only for type-only/app-only code). Phase-3 judge-loop polish notes: center letterboxed replay vertically; player API gaps (preloaded-manifest injection, teal overlay token, missing .d.ts) noted from T3.5 report.
- [x] T3.6 settings + install pages — spec `docs/specs/t3.6-settings-install.md`; visually judged: config editor with dirty-state save bar (persistence verified through D1 round-trip + reload), real key hash in keys table, install page renders the SDK's actual loader snippet, Live verify showed real "Installed — first event seen" from the registry; backend gaps closed (retentionDays update, GET keys); 230 tests green
- [x] T3.7 full-product Playwright e2e — GATE PASSED (5/5 e2e, 237 unit/integration). Spec `docs/specs/t3.7-product-e2e.md`. The gate earned its keep — found and drove fixes for: (a) test-level stale replay-frame handles + page1/page2 copy after seek; (b) `sdkFlushMs`/`sdkFlushLiveMs` TEST_TIMINGS knob (server-driven 15s cadence vs shortened closeMs); (c) REAL BUG: live follow rendered nothing for mid-session viewers → checkpoint-on-join across DO/SDK/player/dashboard (`docs/specs/fix-live-follow.md`, ARCHITECTURE.md §3 "Live join checkpoint"); (d) REAL BUG: dashboard vite proxy lacked `ws: true` — live WebSockets never reached the worker in dev; (e) REAL BUG: player re-armed rrweb `startLive()` on every live frame, resetting the baseline so all post-snapshot events were silently discarded — baseline now anchored once at the last buffered event. Known v1 semantics: a viewer joining an IDLE session waits for the recorder's next activity (checkpoint rides the ingest ack); the "Connected live" state renders on follow intent, not socket-open (judge-loop polish note).
- [x] Phase 3 judge loop — 5 finder lenses → 10 findings adversarially verified (all CONFIRMED) → FIX-P3 (`docs/specs/fix-p3-judge.md`): player live-buffer/overlay bounds, decode-worker restart, gap-seek, reconnect keyframe re-arm, manifest race, 5 registry-UI violations, viewer-connect wide event, live-proxy request-id, presence seed guard, demo-site "Signal Board" rebrand. The old live query credential was replaced by short-lived tickets, and the later Better Auth-only pass removed shared-token project authorization. 240 tests + 5/5 e2e green.
- [x] FIX-P3b replay scale-to-fit — spec `docs/specs/fix-p3b-scale.md`; verified visually with mismatched viewports (whole page scaled 0.56 into the stage, click ripple landing on the recorded button) + geometric e2e assertions green. Verification found two more real bugs: `packages/player` exported `./dist` while every other workspace package exports source — browsers/e2e ran a 3-hour-stale player build (exports now point at src per repo convention; `vp pack` rewrites for publish; if a package change ever seems invisible, check exports→dist staleness FIRST); and the player stamped inline `position:relative` over class-positioned containers by checking inline instead of computed style (collapsed the stage to 0 height → scale math no-op). **Phase 3 COMPLETE.**

### Phase 4 — Packaging, hardening, CI (W4) ✅

- [x] T4.1 self-host packaging — spec `docs/specs/t4.1-selfhost-packaging.md`; mirror script + `--check` verified reproducible, template carries no test vars, guides honest about manual steps/deferred features, README license stays "not yet chosen"; 246 tests green. Note: in-repo template `main` points at `../../apps/worker/src/index.ts` — the published template repo (deferred) will vendor sources; mirror script owns that when publishing lands.
- [x] T4.2 hardening — spec `docs/specs/t4.2-hardening.md`; full audit PASS/FIXED across authz (public surface is now `/api/v1/health` only; `/` returns 404), R2 key validation (incl. sweeper + test routes), input caps (config 64KB, presence string caps), quota-flip verified with no-DO-write test, in-DO append rate limit (429 `rate_limited`, in-memory, TEST_TIMINGS-overridable), nosniff/no-referrer headers, exact-origin CORS, generic 5xx bodies. Live WS now uses 60s HMAC tickets minted over REST — `?token=` rejected (consumer-visible change; player mints per connect/reconnect). Both pinned P3 security notes resolved or documented. 252 tests + 5/5 e2e green (live watch verified under ticket auth). Judge residuals: two stale tests fixed (health probe moved off `/`; raw WS handshake needed Sec-WebSocket-Key/Version for workerd).
- [x] T4.3 observability — spec `docs/specs/t4.3-observability.md`; full coverage table (every route/DO entrypoint/WS handler/queue message/sweep now emits exactly one wide event: `do.live_connect/message/disconnect/error`, `consumer.sweep`, preflight/method-reject/unmatched); version metadata is wired through the logger and Wrangler config (mirror regenerated); identity fields completed; edge→DO→queue→consumer request-id trace asserted in e2e; no stray console; query strings stripped from logged URLs; error fields truncated. 253 tests + 5/5 e2e green. Judge residual: DO debug shape gained firstRequestId — one strict toEqual updated.
- [x] T4.4 CI — spec `docs/specs/t4.4-ci.md`; historical local-first GitHub Actions workflow covered check/test/budgets/e2e/mirror/build with zero secrets. After production moved to Cloudflare Workers Builds and the user chose to avoid duplicate GitHub CI noise, `.github/workflows/ci.yml` was removed. Keep local gates (`vp check`, `vp test`, mirror check, deploy dry-run) as the source of truth before pushing.
- [x] T4.5 final judgment — three full-repo lenses + load exercise, all findings resolved or ledgered:
  - **Conformance**: all 8 core invariants VERIFIED in code (zero-decompression, hibernation-only WS, (tab,seq) idempotency, immutable R2, minimal alarms, sidecar-only metadata, 100KB finalize budget, timestamp-ordered segments). 14 findings triaged → code fixes: manifest served immutable-cacheable (was no-store), deterministic sampling re-check at ingest (shared FNV-1a moved to `@orange-replay/shared/sampling` so honest clients/server can never drift; integration-tested); doc truth-alignment: per-append cost is 2 row writes (~130/session in §7), keepalive-only unload (no sendBeacon — headers), raw-estimate flush trigger, hard-delete retention, 35KB hard bundle ceiling, x-or-tab documented; deferred-list gains explicit entries (edge rate-limiter, orphan-reconcile cron, minimal-sidecar, privacy-tier field, cost tunings).
  - **Privacy**: 5/6 areas PASS; HIGH finding fixed — rrweb Meta events embedded full unscrubbed location.href (query/fragment incl. tokens) in R2 recordings every checkpoint; recorder now scrubs Meta href through scrubUrl(allowUrlParams) at the emit hook (`5d2231b`). DOM-snapshot attrs intentionally untouched (replay fidelity).
  - **Dead code**: removed FLAG_ENCRYPTED, findTransferables block, 4 dead player exports + their orphaned helpers/tests, unused badge.tsx/tabs.tsx (+ its dead tabs primitive dependency), and the DRIFTED hand-written player .d.ts in the dashboard (stale pre-ticket types shadowing the real source types); coupled SDK/DO idle constants documented.
  - **Load probe** (`scripts/load-probe.mjs`, `d95b73a`): 500 + 2000 appends at ~1.4–1.6k req/s local, p50 6–12ms, p95 11–20ms, 250/250 sessions finalized+indexed end-to-end, zero server_error events.
  - Final state: 255 tests, 5/5 Playwright e2e, vp check clean, mirror --check green. **CAMPAIGN COMPLETE — local-first v1 done; deferred list below is the roadmap.**

- [x] Production Cloudflare deployment — `docs/deployment.md` + `apps/worker/wrangler.jsonc` `env.production`; public commits keep placeholder Cloudflare resource IDs and ignored local env files hold generated production values. Created prod D1, R2, KV, queues, Worker secrets, default org/project/write key, and deployed static dashboard assets through the Worker. Production config now uses Wrangler `env.production` and Cloudflare `version_metadata` instead of an `APP_VERSION` var. The production asset build now serves the static `landing/` page at `/`, keeps the dashboard app shell at `/dashboard/index.html`, and routes `/login` plus `/projects/...` through the Worker to that dashboard shell. Secret leak audit passed for current prod token/write-key values in repo files and git history; `scripts/bootstrap-prod-project.mjs` saves generated write keys to the ignored local env file and does not print them. Cloudflare Workers Builds is connected to `imaxisXD/orange-replay` and deploys `main`; GitHub Actions CI is intentionally removed, so Cloudflare's `Workers Builds: orange-replay` check run is the remote deployment signal. Hosted build `1beacec7-da61-4af8-96ef-abbbe780996b` used the pinned Node 22.18 runtime, passed the exact D1/R2 gate, and deployed Worker version `1a7fd942-27d9-4f83-9180-53f25f2eb845`. Final production checks returned 200 for `/`, `/login`, `/or-recorder.js`, health, SDK config, and authenticated sessions; genuine Brave SDK session `019f5d42-f87b-716d-b4be-81b18efc84e8` proved ingest and replay.

- [x] Cloudflare scale hardening (2026-07-09) — dashboard project config now reaches the SDK through `GET /v1/config` before capture starts (remote sampling, mask/block rules, mask policy, canvas toggle); valid KV hits bypass the unknown-key/IP limiter; config KV values persist while edge reads remain cached for 60 s; project/session binding capacity is sized for the 10K concurrent-session target; SDK ingest retries 429 with `Retry-After`; project presence is deterministically spread across 16 DOs and API reads merge the shards. The hosted Worker split was deliberately not implemented: keep the canonical combined deploy until hosted traffic or independent rollout/failure isolation justifies it.

- [~] Public read-only demo workspace (2026-07-09) — `/api/v1/demo` discovers a dedicated demo project without exposing a private account credential; demo auth is allowlisted to session, live, manifest, segment, and live-ticket reads; the dashboard reuses its real sessions, player, and live views under `/demo`; the landing page loads the real SDK with masked recording and clear disclosure. Demo values are stored as Worker secrets and ignored local env values, never in Wrangler vars. `vp check` and all 360 tests pass; Playwright, screenshot comparison, and production smoke checks remain pending.

- [x] Vite+ cache safety (2026-07-11) — replaced workspace-wide `cache: true` with Vite+'s explicit safe split: package scripts are uncached and defined tasks remain cached. This keeps side-effecting `dev`, migration, bootstrap, and deploy scripts from being replayed while preserving task caching. `vp run` loads successfully; two isolated `vp run dev` launches both performed the full startup with `cache disabled`; `vp check` passes with the three existing rrweb-fork warnings; all 500 tests pass.

- [x] Undeployed landing exploration cleanup (2026-07-11) — deleted the unreferenced 1,363-line `landing/live-led-exploration.html` design exploration. It was not part of the explicit deploy asset list and was never present in `apps/dashboard/dist`. `node scripts/build-deploy.mjs` still succeeds and confirms the file is absent; `vp check` passes with the three existing rrweb-fork warnings; all 500 tests pass.

- [x] Exploratory brand asset cleanup (2026-07-11) — deleted 51 unreferenced dashboard logo explorations under `public/brand/saas-inspired` and `public/brand/variants` (511,882 source bytes). Kept all four root final logo files, including the mark used by the app and landing page, so official public brand URLs stay intact. The deploy build succeeds and contains the four root assets with neither exploration directory; `vp check` passes with the three existing rrweb-fork warnings; all 500 tests pass.

- [x] Dead dashboard display-helper cleanup (2026-07-11) — removed 198 lines for timeline bucket/time-to-x, byte-formatting, and active-filter-label helpers that had no runtime caller, along with the two tests that existed only for those dead exports. The remaining focused tests pass (15/15), the dashboard production build succeeds, `vp check` passes with the three existing rrweb-fork warnings, and all 498 remaining tests pass.

- [x] Static dashboard elevation cleanup (2026-07-11) — replaced the unused nested elevation context with the exact merged classes at its only two call sites, then deleted the three helper modules (99 net lines removed). Programmatic class comparison confirms identical outputs. Local browser smoke confirmed the project select and active-key badge retain their backgrounds, 8px radii, and surface shadows with no console errors. The dashboard build drops three modules, `vp check` passes with the three existing rrweb-fork warnings, and all 498 tests pass.

- [x] No-op Analytics Engine ingest cleanup (2026-07-11) — removed the optional `TRENDS` binding and per-batch write path (20 net source lines) because no checked-in Wrangler environment provisions it. Updated the architecture and F4.7 spec to keep the intended one-write-per-finalized-session design. Current Cloudflare types and Wrangler schema confirm the binding/config contract; 33 focused ingest tests, a production Wrangler dry run (no deployment), `vp check` with the three existing rrweb-fork warnings, and all 498 tests pass.

- [x] Guarded test-schema cleanup (2026-07-11) — replaced three exact copies of the same 20-statement D1 setup with one sequential helper (49 net lines removed). Programmatic AST comparison confirms the ordered SQL is unchanged, including the ingest shard lookup. Eight focused integration files pass (80 tests), the production Wrangler dry run succeeds and drops about 7.8 KiB from the raw Worker upload, `vp check` passes with the three existing rrweb-fork warnings, and all 498 tests pass.

- [x] Duplicate SDK worker-entry cleanup (2026-07-11) — removed the unused typed worker runtime, auto-install branch, and its self-only test (154 net lines). The generated Blob-worker source used by production remains exactly 3,288 bytes with the same SHA-256. Six focused SDK files pass (22 tests); the browser build removes 1,211 raw ESM bytes and 955 raw IIFE bytes, both bundles drop about 0.26 KiB gzip and remain inside budget, `vp check` passes with the three existing rrweb-fork warnings, and all 497 remaining tests pass.

- [x] Presence row-mapping cleanup (2026-07-11) — removed the 20-line field-by-field copy after an explicit nine-column SQL select; the row type now extends the returned `PresenceSession` shape and the typed cursor array is returned directly. Three focused presence/API files pass (27 tests), the production Wrangler dry run succeeds without deploying, `vp check` passes with the three existing rrweb-fork warnings, and all 497 tests pass.

- [x] Project-config row validation cleanup (2026-07-11) — removed the separate allowed-origin array validator and kept the existing string-only D1 boundary plus `projectConfigSchema` as the one shape validator (20 net source lines, 26 overall). The smaller test now covers valid, malformed, empty, mixed-type, and non-string inputs; 32 focused ingest tests pass, the production Wrangler dry run succeeds without deploying, `vp check` passes with the three existing rrweb-fork warnings, and all 496 remaining tests pass.

- [x] Integrated local landing-to-live-demo workflow (2026-07-10) — `vp run dev` now builds a stable local asset snapshot and an ignored dev-only Wrangler config before starting the servers. The combined Worker on `8787` serves the real landing page, `/or-recorder.js`, and `/demo`; the separate dashboard server on `5200` remains available for hot reload. `CLEAR_DEV_PORTS=1` now reaches the Vite+ task and stops the old dev process group instead of letting wrappers reopen the ports. Verified in Brave: a fresh landing visit appeared in `/demo/live` and opened as a rendered live replay. `vp check` passes with the three existing rrweb-fork warnings, all 498 tests pass, and the self-host mirror remains current.
- [x] Live session counters (2026-07-10) — the per-session hibernating WebSocket hello now carries the current sidecar-derived timeline and counts, later binary batch indexes update duration/events/errors/rage clicks immediately in the dashboard, and a clean server close refreshes the finalized manifest automatically. This removes the misleading hardcoded live zeroes without adding detail polling or another Durable Object.
- [x] Session detail visual cleanup (2026-07-10) — the sticky demo header now has an opaque base so scrolled page titles do not bleed through it; the timeline rail is wider; rows have usable horizontal padding; and keyboard focus uses a quiet inset amber treatment instead of a cramped edge-to-edge box.
- [x] Replay first-frame rendering fix (2026-07-10) — checkpoint rebasing now keeps the latest viewport event; without it rrweb left the first iframe hidden at `0×0`, which caused the black screen after refresh. The temporary first-frame loader was removed after the underlying bug was fixed. Verified in Brave with refresh → Play through 12 seconds; `vp check --no-fmt` passes with the three existing rrweb-fork warnings and all 500 tests pass.
- [x] Fluid dashboard scrollbars (2026-07-11) — installed Fluid Functionalism's Base UI `scroll-area` registry item and replaced the dashboard page scrollbar plus app-owned vertical and horizontal scroll areas in session lists, replay controls, journey breadcrumbs, timeline events, metric rails, and the install snippet. Vendored the documented shadcn `scroll-fade` and `scroll-fade-x` CSS utilities into the dashboard stylesheet: 48px edge masks with scroll-driven animation keep real start/end edges crisp. Touch-primary devices keep native momentum scrolling through the component's built-in fallback. The sessions rail is shortened to `calc(100vh - 370px)` so the sessions page itself stays fixed while only the rail scrolls. Brave verification confirmed the page does not move, the rail shows both fades and the Fluid thumb while scrolling, and the documented component has no separate fade arrow. `vp check` passes with the three existing rrweb-fork warnings and all 500 tests pass.
- [x] SDK large-DOM snapshot and main-thread stall fix (2026-07-12) — frame-by-frame tracing first found synchronous canvas/image encoding in the full DOM walk; the recorder now seals those pixels later with bounded `toBlob()` work. Full snapshots capture topology, serialize, reconcile mutations, filter attributes, and recheck privacy in short paint-aligned slices. The main document is delivered first; same-origin iframe documents are then captured parent-first, one at a time, after the sink confirms capacity, so multiple snapshots larger than 4MB cannot accumulate or repeatedly hit the limit. Mutation coalescing is O(new changes) with exact byte deltas, large structural batches abort stale work before enumeration, nested iframe cleanup is O(n), and an aborted snapshot cannot leave live mirror IDs behind. Snapshot trees move to the compression worker in byte- and 256-node-bounded chunks, and the worker writes deeply nested JSON incrementally instead of rebuilding and stringifying one giant object. One snapshot part may exceed the normal 4MB cap while retaining at most 128KB of live catch-up; structural overflow flushes the valid catch-up, drops the gap, then forces a new checkpoint without disabling recording. Failed required uploads discard dependent events and force a full replacement checkpoint; this also covers synchronous, delayed keepalive, in-flight iframe, and BFCache-restore races. Worker creation or runtime failure stops capture instead of moving the same work back onto the page; CSP sites can still choose explicit `transport: "inline"`. Chromium passes all six snapshot cases, including 10,000 nodes, 100,000-node page/iframe cases at 4× CPU slowdown, and a 20,000-node page with two separate 100,000-node iframes; both CSP cases pass too. All 617 tests pass. Deterministic audited builds keep the public API and replay wire keys unchanged at 34.86KB ESM, 34.94KB IIFE, and 0.91KB loader gzip, within the unchanged hard limits. The SDK format, lint, and type checks are clean, and three independent correctness/regression/performance reviews approve the final code. The vendored rrweb check remains better than its checked-in baseline (53 errors/40 warnings now versus 73/42 at `HEAD`), while the untracked `.agents/` skill files and unrelated dirty dashboard replay controls still prevent a clean unscoped `vp check`.

- [x] Sessions critique polish (2026-07-12) — fixed all five Impeccable findings: narrow screens now use a list-to-replay flow with an explicit Back action; session cards show clicks/pages or a metadata-only label; “Most friction” is a real server-side sort; helper text meets the muted-foreground contrast floor; watched state starts only when replay playback or a live connection begins and includes Undo/Mark unwatched; listbox/option semantics, loading feedback, copyable session IDs, and retry/next/back recovery actions are in place. The session rail was then redesigned in Impeccable Live using the accepted aligned-grid variant: the decorative activity chart is gone, healthy rows use two scan-friendly lines, and only sessions with useful evidence add a third line for clicks, pages, errors, and rage clicks. Browser checks passed at 390×844, 1440×900, and the 320px rail width with no hidden horizontal overflow; selection/play/undo/sort behavior and both healthy and friction rows were exercised against seeded demo data. The Impeccable detector reports no findings for the Sessions route; dashboard `vp check` passes with no warnings, the dashboard production build succeeds, and all 502 tests pass. The untracked `.agents/` skill files still prevent a clean unscoped `vp check` and can also make Vite+ panic while printing their warnings.

- [x] Complete Base UI primitive migration (2026-07-13) — moved the shared Fluid Select, Button, Switch, Tooltip, and Scroll Area foundations to Base UI while keeping their public component APIs, sizes, colors, shadows, focus treatment, and motion. Removed every former primitive dependency; executable source, dashboard dependencies, and the lockfile now use Base UI only. The combobox keeps its stacked-chevron spring and pointer/keyboard focus split. Computer Use checks in Brave confirmed select mouse/keyboard behavior, normal and link-style button actions, switch toggling by mouse and Space, and tooltip opening from keyboard focus with the same Fluid styling. The dashboard source check passes all 97 source files and the production build succeeds. The final combined tree now passes `vp check` across 520 formatted files with zero errors and only four known rrweb warnings; all 815 tests pass.
- [x] Honest production environment label (2026-07-13) — the dashboard header no longer hardcodes `Local dev` for every non-demo build. A small covered helper keeps `Demo` for the public demo and shows `Production` only when the production build path marks the bundle; the normal dev server and the built local Worker both stay `Local dev`. The connected-environment card reports the Worker and Better Auth account session. The first hosted build exposed Cloudflare's default Node 22.16 below this repo's 22.18 minimum, so the Worker build root now pins Node 22.18. PRs #3 and #4 are merged, the hosted build ran on Node 22.18, and Brave confirmed the environment labels before the later auth migration.

- [x] Semantic Orange Replay toast family (2026-07-13) — the Base UI toast now supports default, success, error, warning, info, and loading states while keeping its 48px height, graphite surface, dashed border, compact corners, and sharp Orange Replay pixel identity. The selected amber/teal logo sheet is paired with state-specific sheets; actual opaque bounds align each visible fragment to the card edge. The provider keeps Base UI's normal three-visible-toast limit and five-second timeout, promise toasts resolve in place, and the authenticated toast lab covers individual and mixed stacks. The resting stack now follows Base UI's own pattern: the front toast stays readable while older toasts become 90% and 80% scaled peeks with hidden content. Hover or keyboard focus expands all three into full 48px cards with 8px gaps, and the transparent gap bridge keeps the stack open while the pointer moves between cards. Promise completion uses one readable storyboard: the loading text exits for 150ms, the spinner and check crossfade for 250ms while the semantic colors change, the ready text enters on the next paint, and the success particles restart once without replacing or resizing the Base UI toast. Only the front toast mounts the four one-shot particle layers, so hidden stacked toasts do no particle animation work. Their movement finishes after about one second, then the fragments stay still and leave through the toast's parent opacity so particles and message disappear together. Reduced-motion mode removes positional motion, transition waiting, and decorative particles. Desktop, 390×844, eight-click burst, action navigation, asset, offset, completion, synchronized exit, auto-dismiss, collapsed stack, hover expansion, gap traversal, focus expansion, and frame-sampled loading-to-ready checks passed.
- [x] Always-visible toast gallery (2026-07-13) — the toast lab keeps its existing trigger list and now renders default, success, error, warning, info, and loading previews below it. Static previews reuse the live toast's signal, message, semantic edge, action, close affordance, and four particle layers, but do not create fake buttons or use Base UI dismissal timers. The gallery uses two columns on desktop and one column on narrow screens. Component coverage proves all six remain mounted after 20 seconds; browser checks confirm the same after the normal five-second toast timeout, with no horizontal overflow at 390×844 and no console errors.

- [x] Combined release verification and replay journey hardening (2026-07-13) — waited for the toast work to finish, then reviewed the complete shared worktree before staging. The recorder's existing page-load vital now carries the scrubbed page URL for both direct and loader starts; replay journeys use that signal for normal full-page moves without inventing `nav` events, changing navigation counts, adding polling, or adding Worker writes. URL-bearing page loads stay protected by timeline budgets. Demo E2E setup now supplies explicit valid local auth values, reads stored segment batches through the real wire wrapper, follows the current Sessions split-view flow, and isolates the no-op save used to prove dead-click detection. Intended-file `vp check` covers 440 files with 0 errors and the 4 known rrweb warnings; all 625 tests and all 13 browser E2E cases pass. The self-host mirror, dashboard and Worker builds, combined deploy build, SDK budgets (34.88KB ESM, 34.97KB IIFE, 0.91KB loader gzip), and production dry run pass. No production deploy was run; the existing `REPLACE_WITH_PRODUCTION_KV_ID` Wrangler placeholder still needs the real production KV ID before deployment.

- [x] R2 analytics warehouse and safe cutover (2026-07-13) — implemented the full analytics path described in `docs/specs/f4-r2-analytics-cutover.md`: D1 stays the control plane, finalized session/event rows leave through a durable outbox and typed Cloudflare Pipeline, R2 Data Catalog/Iceberg stores analytics history, and R2 SQL serves bounded dashboard reads. The same structured filter opens the exact matching session set. This also fixes page counting, adds sidecar-derived rage/max-scroll/internal-quick-back/interaction-time fields, enforces residency again at send time, adds stale-data warnings and pinned pages, and provides exact D1-versus-R2 acceptance, backfill, compare, rollback, deletion, and Spark purge tools. Deployment now checks compare and R2 synchronously with the exact candidate token, uploads code and secrets in one version, keeps Cloudflare account credentials out of application builds and smoke checks, and prepares a tagged inactive D1 version before every normal deploy. Emergency rollback promotes that prepared version without reading the current checkout or dashboard assets; the source-rebuild rollback remains a named second choice. Independent security, migration, compare, retention, and deployment reviews drove these fixes. Final local gates pass: all 520 files are formatted, `vp check` has zero errors and only four existing rrweb-fork warnings, 815/815 tests pass, and the full six-package build, self-host mirror check, production asset build, secret scan, and real-resource dry runs pass for both the R2 version and prepared D1 fallback.

  Production migrations 0006–0009 are applied. The bucket `orange-replay-analytics-prod`, typed stream, three Data Catalog sinks, and Pipeline are live; the separate bucket-scoped R2 SQL reader and catalog-maintenance tokens replaced the broad setup credential, and the purge runner values are in the protected `production-analytics` GitHub environment. The fixed-cutoff backfill exported all four existing sessions and five identities, the second apply inserted zero, and exact acceptance matched D1 and R2. Compare matched, R2 SQL cutover passed, a real D1 rollback drill passed after propagation, and R2 SQL was restored. A real retention probe then finalized through the Durable Object, queue, recording bucket, Pipeline, and R2 SQL. It exposed Cloudflare R2 SQL error 40018 in the watermark visibility query; the redundant sink-enforced null checks are now live in Worker version `d351e5cb-36aa-4da7-9537-c1de6a886eaa`. Production API and D1 analytics doorway smokes pass, the watermark reached probe export sequence 13 with no error, and the exact guarded expiry changed only the probe row. The real scheduled retention pass removed that probe from D1, deleted all four replay objects, published deletion sequence 14, and left the original four database rows plus eight replay-object hashes unchanged. The watermark reached 14 with no error, the authenticated API hides the probe, and production is back on R2 SQL after a second full acceptance match plus a successful source-independent tagged D1 rollback drill.

  The first GitHub/Spark physical purge safely exposed one real-infrastructure bug: the row deletes succeeded, but the nested `rewrite_data_files` filter lost its value quotes. The escaped filter fix merged in PR #2; runs `29283337755` and `29284127445` then completed that probe with two zero checks more than 10 minutes apart, no error, and no lease. A second proof used genuine current SDK session `019f5d42-f87b-716d-b4be-81b18efc84e8`: it finalized with five batches and 8,978 replay bytes, reached R2 as warehouse version 15, and appeared in the exact 24-hour metric doorway with two pages, eight clicks, one TypeError, 100% max scroll depth, and 15 seconds of interaction time. Brave rendered both recorded pages, masked form data, nine timeline events, Play/Pause, first-error seek, and the page-journey seek. The guarded normal deletion path removed one D1 row and all three replay objects, published tombstone sequence 16, hid the replay in the API and dashboard, and returned exact acceptance to D1 4 = R2 4 while all eight original replay objects kept their hashes. Physical runs `29287641005` and `29288355294` deleted one session row plus 15 event rows, then proved zero rows again after more than 10 minutes; `purge_attempts = 2`, `completed_at` is set, `purge_last_error` is null, and no lease remains. The purge workflow now pins official action releases that declare the Node 24 runtime, instead of relying on GitHub to force actions that target Node 20 to run on Node 24.

- [x] Continuous live-to-recorded session handoff (2026-07-14) — spec `docs/specs/fix-live-session-continuity.md`. A recording now appears in Sessions after its first accepted batch and keeps one session ID, row, URL, and player through `Live`, `Final details pending`, and exact finalization. R2 SQL remains the exact analytics list; a private bounded and indexed D1 plus 16-shard Presence overlay bridges open and finalizing sessions without turning D1 into an analytics database. Idle recordings stay playable and seekable from received segments and pending batches; finalization swaps in the immutable manifest without clearing the visible frame. The SDK shares the server's then-current 30-minute boundary, safely reconciles dormant tabs, preserves each tab's sequence, and uses random UUIDv7 bits for copied-tab IDs. Review fixed the failed-ticket player lock, same-session sequence reset, copied-tab collision, finalization races, and 100,000-node recorder stalls. Final local gates: `vp check` has 0 errors and 4 known warnings, 878/878 tests pass, 13/13 browser E2E cases pass, and the six package builds, mirror, combined assets, SDK budgets (34.74KB ESM, 34.83KB IIFE, 0.91KB loader), and both Wrangler dry runs pass. Manual in-app browser QA showed Live → pending → exact on the same URL with zero console errors. This work is local only: migration `0010_session_head_index.sql` has its existing-row `indexed_at = ended_at` backfill but has not been applied to production, and no deploy was run.

- [x] GitHub hosted auth and account-owned project keys (2026-07-13) — Better Auth now runs inside the combined Cloudflare Worker with native D1 storage, GitHub-only sign-in, exact trusted origins, database rate limiting, encrypted OAuth tokens, and no password sign-up. Signed-in users get a personal workspace and default project; workspace membership protects every dashboard API, and owners/admins can create named write keys whose plaintext is shown once, list audit details, and revoke them. D1 is the durable revocation source before central KV deletion; failed deletions keep a pending-sync marker and retry from the key list plus a five-minute scheduled repair. Every active KV writer first creates a durable D1 job, final checks stay scheduled around unfinished writers, and each checked job moves behind newer work so a stopped request cannot starve the fixed repair queue. Bootstrap keys also use this repair path instead of writing KV directly. Cloudflare KV can still have a short edge propagation window. Key writes are limited to 30/minute/user/project, project history is capped at 100 rows, and revoked audit rows plus KV entries expire after 90 days. The public `/demo` route remains anonymous and read-only. The first-party `/_admin` page shows user/workspace/project/key counts and supports the narrow user, role, ban, and session actions allowed by the Worker. Migrations `0011_hosted_auth.sql` through `0014_key_cache_write_jobs.sql`, legacy workspace linking, admin promotion, bulk production-secret upload, safe deploy ordering, self-host mirror, and the hosted-auth/deployment runbooks are included. The 451-file app/repo check passes with four existing rrweb-fork warnings, all 669 tests pass, the dependency audit reports no known production vulnerabilities, the combined production build and Wrangler dry run pass, and two independent final security reviews found no remaining cache-race issue. A live local browser check confirmed GitHub mode, anonymous demo access, and signed-out `/_admin` redirect with no console errors. The bare root check still scans the unrelated untracked `.agents/` skill folder, so those files were left untouched. No production deploy or real GitHub callback was run; production still needs the real GitHub OAuth App, Cloudflare resource IDs and secrets, migration/deploy, first account link/promotion, and a real sign-in plus key create/revoke check.

- [~] Shared dashboard loading matrix (2026-07-13) — every dashboard skeleton, custom spinner, spinning icon, replay wait indicator, install check, button progress state, admin row action, and loading toast now uses one `gradient-spin` wrapper. Its settings match the chosen preview exactly: `sunrise`, `snake`, 750ms, dim 0.07, 3 rows, 5 columns, 4px cells, and 2px gaps. Reduced-motion behavior remains enabled. The old skeleton component and spinner CSS were removed. Dashboard lint and type checks pass, all 114 dashboard tests pass, and the dashboard production build succeeds. Browser checks confirmed the 28×16 matrix is centered and unclipped in page, button, and toast loading states. The bare root check remains blocked by unrelated existing formatting and lint findings, mainly under the untracked `.agents/` folder. The full suite passed 791/794 tests; three Worker finalization tests timed out outside this dashboard work, and a one-worker retry passed e2e but still had one shifting consumer timeout. Full local startup also remains blocked by the existing local D1 error `table users already exists`; no migration or local database was changed.

- [x] Public analytics and curated replay pages (2026-07-14) — project managers can publish one search-friendly SSR page from Settings, see and copy its public address, and explicitly share up to 10 finalized recordings. The existing combined Worker serves the React SSR document, public JSON, rewritten replay manifests, opaque replay segments, and the separate public client bundle; TanStack Query hydrates and refreshes the analytics without adding another publish Worker or SSR framework. Random public IDs, immediate unpublish and recording revocation, strict project checks, no private IDs or live-session data, no-store responses, rate limits, CSP, and safe public error pages protect the public surface. D1 migration `0015_public_pages.sql`, the production custom-domain configuration, deployment docs, and the full spec are included. Desktop and mobile browser checks covered the public page, lazy replay player, Settings card, and recording picker with no console errors or horizontal overflow. Public-page, dashboard, and Worker checks pass; D1 matches Drizzle across 23 tables; all 813 tests pass; the combined asset build, Worker dry run, and dummy production custom-domain dry run pass. No production deployment was run. The bare root `vp check` still stops only on existing unrelated formatting under `.agents/`, `apps/dashboard/src/lib/filter-chips.ts`, and `apps/worker/tests/no-replay-decompression.test.ts`; those files were left untouched.

- [x] Main synchronization and local D1 history recovery (2026-07-15) — fast-forwarded local `main` across all 17 missing upstream commits, then reapplied and reviewed the hosted-auth, public-page, loading, and deployment work against the current R2 analytics and live-session architecture. Canonical migrations now run from `0009_analytics_warehouse.sql` through `0015_public_pages.sql`. The local-only migration repair recognizes the two previous hosted-migration filename histories, compares the exact existing table and index definitions—including types, defaults, constraints, and index columns—against a fresh schema built from the checked-in migrations before changing Wrangler's ledger, fills the one known old analytics-column gap, and refuses mixed, ambiguous, or altered histories; remote migration commands never run this repair. A byte-for-byte backup was made before repair, all 895 pre-existing local sessions were preserved, and the browser journey added one new test recording. D1 integrity and the 23-table Drizzle comparison pass, a second local migration run is a no-op, all 951 tests pass, the intended 594-file check has zero errors and four known rrweb warnings, package/deploy builds and SDK budgets pass, and manual browser QA proved one stable session through pending to exact replay with no console errors. No production migration, deploy, push, or secret change was run.

### Post-v1 product backlog (strategy: `docs/product-moat.md`)

Ranked moat features (each with pain/mechanism/build sketch in the doc): 1) edge injection — zero-code install for CF-proxied zones; 2) everything-buffer + promotion rules (record 100%, 48h, promote what matters); 3) share links / unlimited viewers (scoped session-read tickets + public player route); 4) presence API + co-browse; 5) provable privacy report; 6) ephemeral per-PR replay stacks. Player roadmap in the same doc — **known player gap: multi-tab sessions interleave into one replayer (verified: `mergeReplayEvents` is a timestamp sort, tab identity dropped at decode) — first fix is a tab picker**; then clip export (client-side canvas→WebM), comments, dead-click markers, friction heat-lane.

**AI-era + player-depth specs written and ready to dispatch (2026-07-04):**

- [ ] **F1 — Replay-to-Repro** (`docs/specs/f1-replay-to-repro.md`): any error session → a runnable Playwright regression test + machine-readable failure bundle + markdown report, generated **client-side** (no server inspection — privacy-preserving). New `packages/player/src/repro/` pure module (no rrweb/DOM imports so F2 can reuse it in Node). The differentiated AI move: replay → failing test → hand to a coding agent.
- [ ] **F2 — MCP server** (`docs/specs/f2-mcp-server.md`): `packages/mcp` (`@orange-replay/mcp`), a local stdio MCP bridge exposing 5 tools (list_sessions, get_session_timeline, get_failure_bundle, get_repro_script, list_live_sessions) so the user's own Claude Code/Cursor pulls production replay evidence + F1 repro artifacts. New dep: `@modelcontextprotocol/sdk`. Reuses F1's pure repro module via Node segment decode. **Depends on F1.**
- [ ] **F3 — Player polish** (`docs/specs/f3-player-polish.md`): dead-click detection + hollow-ring markers, louder overlay effects (bigger amber ripples, cursor trail, rage rings), activity heat strip on the scrubber (manifest-only, privacy-clean), journey breadcrumbs, jump-to-first-error. All from existing capture; no SDK/worker changes. Addresses the "effects too subtle / no wow / lacks competitor depth" feedback.
- [ ] **F4 — Analytics dashboard** (`docs/specs/f4-analytics.md`, written 2026-07-10 after a source-checked Clarity gap review): Clarity-class overview in phases — F4.0 metric correctness (replace the broken `url_count` product meaning with covered, per-tab `page_count` + shared `SessionFilter`), F4.1 stats endpoint + overview page on existing D1 data, F4.2 sidecar-only insights reusing the player's existing rage detector through `@orange-replay/shared` (no SDK release), F4.3 SDK capture v2 (visitor ID, referrer/UTM, web vitals, active time, dead clicks, `identify`/`track`), F4.4 users/attribution/performance cards, F4.5 heatmaps, F4.6 smart events + funnels, F4.7 AE trends. Hard rule: every metric opens its exact session set. Overlaps F3 on dead-click definition — whichever lands second reuses the first's definition verbatim.
- Suggested order: **F3 first** (fast, fixes the visible-quality complaint), then **F1**, then **F2** (needs F1). F4 phases can interleave: F4.0+F4.1 are independent of F1–F3 and ship the first visible analytics. Not yet dispatched — awaiting go.

### Deferred (needs real CF account or user decision — do NOT pick these up)

Analytics Engine provisioning and finalization-side trend writes · Vectorize/AI · heatmaps UI backend · console/network lazy capture chunks · processing lane · E2E-encryption tier · privacy-tier config field · BYOC provisioner · template publishing · orphan-reconcile cron (re-emit finalize for R2 prefixes without D1 rows) · minimal-sidecar mode · compressed-estimate flush trigger + per-append state-write collapse (cost tuning) · **OSS license (user decision pending)**.

## Working protocol (how tasks get executed)

1. Orchestrator writes a precise spec (UI tasks: per-element, derived from `docs/design-language.md`) and saves it to `docs/specs/t<N>-<slug>.md`.
2. Implementation goes to **Codex CLI** (`gpt-5.5`, xhigh — the account config defaults; do NOT pass model flags): detached run, `cd` to repo root inside the wrapper, `--sandbox workspace-write -c sandbox_workspace_write.network_access=true`, done-marker file + stall watch (no log growth ≥ 20 min ⇒ kill and resume). A startup `rmcp … AuthorizationRequired` ERROR line in the log is cosmetic; judge health by log growth.
3. Codex sandboxes cannot run workerd/Playwright — the orchestrator runs the full suite after.
4. Judge loop per phase (PLAN.md §Execution model): finder lenses → dedupe → adversarial verification → fix rounds (max 3).
5. UI is judged visually: boot worker + dashboard, screenshot each screen, compare against `design-final.html`.

## Runbook facts (local dev)

- Gates: `export PATH="$HOME/.vite-plus/bin:$PATH" && vp check && vp test` from repo root.
- Full local dev: `vp run dev` from repo root. One integrated Cloudflare and Vite server at `http://localhost:8787` serves the landing page, live dashboard source, `/demo`, Worker APIs, local bindings, and hot reload. Set `CLEAR_DEV_PORTS=1` only when you want the script to replace an existing local app first. Dev auth uses Better Auth with the GitHub OAuth values from ignored `apps/worker/.env` (copy tracked `.env.example` first). Tests disable local env loading, so the local `.env` cannot override their explicit test values.
- Worker only: `vp exec --filter @orange-replay/worker -- wrangler dev --port 8787`.
- Dashboard development uses the full local command because its APIs, auth, and Cloudflare bindings share the same server. Private dashboard routes use GitHub sign-in through Better Auth. A missing or partial Better Auth setup fails closed; the anonymous `/demo` remains separate.
- Seeded demo data: worker `/__test/*` seed routes populate sessions matching the design mock.
- D1 `exec()` splits on newlines — keep multi-line SQL single-line in migrations run through it.
- Design reference server: `python3 -m http.server 5099` at repo root → `http://localhost:5099/design-final.html`.
