{
  "name": "@orange-replay/worker",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build:deploy": "cd ../.. && node scripts/build-deploy.mjs --production",
    "dev": "wrangler dev",
    "deploy:prod": "cd ../.. && vp run deploy:prod",
    "deploy:prod:dry-run": "cd ../.. && vp run deploy:prod:dry-run",
    "test": "vp test",
    "check": "vp check",
    "build": "wrangler deploy --dry-run --outdir dist"
  },
  "dependencies": {
    "@orange-replay/shared": "workspace:*"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250620.0",
    "@types/node": "catalog:",
    "drizzle-kit": "catalog:",
    "drizzle-orm": "catalog:",
    "typescript": "catalog:",
    "vite": "catalog:",
    "vite-plus": "catalog:",
    "wrangler": "^4.20.0"
  }
}
