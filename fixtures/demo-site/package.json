{
  "name": "@orange-replay/demo-site",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "e2e": "playwright test --config playwright.config.ts",
    "e2e:sdk": "playwright test --config playwright.sdk.config.ts",
    "profile:dashboard": "node scripts/profile-dashboard.mjs"
  },
  "dependencies": {
    "@orange-replay/sdk": "workspace:*",
    "@orange-replay/shared": "workspace:*"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1",
    "playwright": "^1.62.1"
  }
}
